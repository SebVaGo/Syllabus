import { Typography } from '@mui/material';

const ModuleTitle = () => {
  return (
    <Typography
      variant="h3"
      sx={{
        color: '#3E5060',
        fontWeight: 'bold',
        mb: 4,
        textAlign: 'center',
      }}
    >
      DropDown
    </Typography>
  );
};

export default ModuleTitle;
